import { KaCreditsPipe } from './ka-credits.pipe';

describe('KaCreditsPipe', () => {
  it('create an instance', () => {
    const pipe = new KaCreditsPipe();
    expect(pipe).toBeTruthy();
  });
});
