import { KaPaymentMethodPipe } from './ka-payment-method.pipe';

describe('KaPaymentMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new KaPaymentMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
